default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("DATABASE_POOL", 5) %>
  url: <%= ENV.fetch("DATABASE_URL", "postgres://starter:starter@localhost:5432/starter_development") %>

development:
  <<: *default

test:
  <<: *default

production:
  <<: *default
